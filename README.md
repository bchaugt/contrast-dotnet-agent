# contrast-dotnet-agent

This cookbook will install a Contrast Security .NET agent with the following steps:
1. Download the .NET from the target Contrast organization via API
2. Execute a silent install of the Contrast .NET agent
